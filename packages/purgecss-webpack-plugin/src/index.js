


class PurgecssWebpackPlugin {

    constructor(options) {

    }

    apply(compiler) {
        compiler.plugin("compilation", compilation => {

            compilation.plugin("optimize", () => {
                
            })
        })
    }

}